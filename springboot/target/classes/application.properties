spring.application.name=campusflow

# Application URLs
# Backend API : http://localhost:5000/api
# Frontend    : http://localhost:3000

# Database
spring.datasource.url=jdbc:mysql://localhost:3307/campusflow?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=campusflow123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=campusflow_jwt_secret_key_change_this_in_production_min_32_chars
app.jwt.expiration=604800000

# Server
server.port=5000
